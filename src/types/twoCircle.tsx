export type TwoCircle = {
  /** 원의 색상 (Tailwind 색상 클래스) */
  circleColor: string;
  /** 중앙에 표시될 텍스트 */
  text: string;
  /** 텍스트 색상 (Tailwind 색상 클래스) */
  textColor: string;
  /** 텍스트 크기 */
  textSize: number;
  /** 전체 크기 (width와 height) */
  size: number;
};
